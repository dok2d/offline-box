---
##############################################
# Credentials & secrets
# Override these in host_vars, vault, or -e
##############################################

# Transmission
transmission_rpc_password: "{{ lookup('password', '/dev/null chars=ascii_letters,digits length=24') }}"

# Nexus
nexus_admin_password: "{{ lookup('password', '/dev/null chars=ascii_letters,digits length=24') }}"

# Nextcloud
nextcloud_admin_password: "{{ lookup('password', '/dev/null chars=ascii_letters,digits length=24') }}"

# Jellyfin
jellyfin_admin_password: "{{ lookup('password', '/dev/null chars=ascii_letters,digits length=24') }}"

# Syncthing
syncthing_gui_password: "{{ lookup('password', '/dev/null chars=ascii_letters,digits length=24') }}"

# Paperless-ngx
paperless_ngx_admin_password: "{{ lookup('password', '/dev/null chars=ascii_letters,digits length=24') }}"
paperless_ngx_secret_key: "{{ lookup('password', '/dev/null chars=ascii_letters,digits length=48') }}"

# Gitea
gitea_admin_password: "{{ lookup('password', '/dev/null chars=ascii_letters,digits length=24') }}"
gitea_secret_key: "{{ lookup('password', '/dev/null chars=ascii_letters,digits length=48') }}"

# SearXNG
searxng_secret_key: "{{ lookup('password', '/dev/null chars=ascii_letters,digits length=48') }}"

# Vaultwarden
vaultwarden_admin_token: "{{ lookup('password', '/dev/null chars=ascii_letters,digits length=48') }}"
